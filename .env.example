# Copy to .env or configure these values in your deployment environment.
# Never commit real values.

TELEGRAM_BOT_TOKEN=
GOOGLE_SHEETS_ID=
GOOGLE_SHEETS_RANGE=Sheet1!A2:J

# Google service-account credentials are read from credentials.json by sheets.py.
# Keep credentials.json outside Git and provide it securely at deploy time.
